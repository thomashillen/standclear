# Routine log

One line per autonomous run: date/time · mode · branch or PR · summary.

---

2026-05-13 13:20 UTC · MODE B · claude/keen-goldberg-oh5NH · `app/sitemap.ts` listed `/status` with `priority: 0.5, changeFrequency: "daily"` while `app/status/page.tsx` sets `robots: { index: false, follow: true }` — the same maintainer-on-shift run (2026-05-12 13:19) that fixed the /status static-data bullet implicitly relied on /status being noindex but the sitemap kept submitting it for indexing. That mismatch is the canonical "Submitted URL marked 'noindex'" SEO antipattern Google flags in Search Console: the crawler fetches the URL only to discover it shouldn't index, wasting crawl budget against the real station + line pages. Dropped the /status entry; left an inline comment anchoring the rationale so a future re-add trips the same reasoning. New `app/sitemap.test.ts` (node-env, 5 cases) pins the contract: /status is excluded (exact URL + pathname-leaf belt-and-suspenders so a hypothetical `/status/foo` surface also trips it), the canonical marketing surfaces (root, /about, /changelog, /privacy, /terms) are present, dynamic /line/* and /station/* entries each have ≥1 row (guards against a build that lost public/gtfsData.json), every entry carries a Date lastModified, and URLs are globally unique (the two sources are concatenated; a future edit that adds the same path to both halves would silently double-list). Test file exercises `getAllStationsServer` / `getLinesServer` through `sitemap()` so `lib/stations.server.ts` picks up indirect coverage. Side fix: `vitest.config.ts` aliases the `server-only` bundler-hint package to a stub at `vitest.server-only-stub.ts` (no real package on disk — Next provides the throw-on-client-bundle behavior at build time), unblocking any future node-env test that imports a `server-only`-marked module.
2026-05-12 13:19 UTC · MODE B · claude/status-static-check-copy-2026-05-12 · /status "What this measures" bullet for Static data was stale after PR XqULa (2026-05-12 00:25) made the check a real fs.statSync probe of `public/gtfsData.json` — the copy still said "Sentinel today; future checks will probe the edge cache." Rewrote the bullet to describe what the probe actually verifies (presence + minimum size ≥100 KB) and why it matters (a deploy that dropped the build step flips the rollup instead of silently rendering empty panels). The StatusPanel below already surfaces the actual KB size from `check.detail`, so a rider looking at the live page now reads consistent copy + data instead of explanatory text that disagrees with the data block. Copy-only edit to a non-indexed page (robots: noindex on /status) — no changelog entry, consistent with PR #104 precedent for marketing-surface accuracy fixes; no test added, no test asserts /status static-bullet copy.
2026-05-09 12:15 UTC · MODE B · claude/nice-hopper-nQcmG · proxy Mapbox geocoding + walking-directions through /api/geocode + /api/walk so MAPBOX_TOKEN never reaches the browser
2026-05-09 12:25 UTC · MODE A · claude/nice-hopper-nQcmG (PR #38) · add per-IP sliding-window rate limiting to geocode + walk proxy routes; addresses Codex P1 comments on open billing proxy risk
2026-05-09 13:10 UTC · MODE B · claude/keen-goldberg-UK9md · per-station OG cards: build-time PNG per slug with station name + MTA route bullets so a tweeted station link gets a custom card
2026-05-09 13:45 UTC · MODE B · claude/keen-goldberg-tFvbg · per-page OG cards for /about, /pricing, /changelog so each marketing surface gets its own framed social thumbnail
2026-05-09 14:00 UTC · MODE B · claude/per-train-staleness-2026-05-09 · per-train marker fade driven by GTFS-RT VehiclePosition.timestamp so a vehicle that hasn't reported in 90s+ visibly dims even when the snapshot itself is fresh
2026-05-09 14:15 UTC · MODE B · claude/line-landing-pages-2026-05-09 · per-line landing pages at /line/[id] mirroring /station/[slug] — every train (1–7, A/C/E, B/D/F/M, G, J/Z, L, N/Q/R/W, shuttles, SI) has a static SEO surface that links into the live map at ?line=&lt;id&gt;
2026-05-09 16:15 UTC · MODE B · claude/press-page-2026-05-09 · add /press page (boilerplate, quick facts, brand assets, screenshot URLs, contact) + matching OG card; sitemap + changelog + footer link updated
2026-05-09 17:15 UTC · MODE B · claude/respect-reduced-motion-2026-05-09 · drop `essential: true` from cinematic follow-mode easeTo calls (entry tilt, exit unwind, per-tick recenter) so riders with prefers-reduced-motion get tracking without the camera-lean animation, per product principle #3
2026-05-09 18:00 UTC · MODE A · claude/fix-search-geocode-2026-05-09 · user reported "no results" typing "550 madison": the geocode proxy was silently swallowing 5xx (e.g. unset MAPBOX_TOKEN) into a generic empty list. Plumb an onError signal through makeDebouncedSuggester and surface a discrete amber notice in SearchSheet so riders can distinguish "no match" from "address search is down".
2026-05-09 18:15 UTC · MODE A · claude/geocode-public-token-fallback-2026-05-09 (PR #50 follow-up) · user confirmed the notice rendered but search still broken on prod — MAPBOX_TOKEN unset in Vercel. Make /api/geocode + /api/walk fall back to NEXT_PUBLIC_MAPBOX_TOKEN with an operator-log warning, so an unconfigured deploy stays functional rather than silently 503-ing.
2026-05-09 18:45 UTC · MODE B · claude/forward-client-errors-2026-05-09 · client-side error/warn records now POST to /api/log via sendBeacon (fetch keepalive fallback) and are re-emitted through the server logger so they land in Vercel function logs. Route is rate-limited, body-capped, sanitizes URLs and truncates fields; client side has a per-page-load budget to defend against render loops.
2026-05-09 19:00 UTC · MODE B · claude/follow-capsule-staleness-2026-05-09 · textual companion to the per-train marker fade: a small pure helper classifies VehiclePosition.timestamp into fresh/soft-stale/hard-stale bands; FollowCapsule swaps the "NEXT STOP" eyebrow for an amber "Updated Nm ago" / "Stale · Nm" line when the rider's followed train hasn't reported in 90s+. Snapshot generatedAt fallback preserved for silent-feed outages.
2026-05-09 20:15 UTC · MODE B · claude/trip-sibling-routes-2026-05-09 · trip planner now records co-running routes (N/R/W on Broadway BMT, 4/5 on Lex express) as per-leg siblingRouteIds during path-dedup. TripPlanRow renders all sibling bullets in the leg ribbon, the "Next:" list shows per-arrival route bullets so the rider sees whether to board a W or an N, and estimateTripTimeSec's live-wait lookup matches sibling routes too — so the soonest sibling arrival drives the total-min pill rather than getting hidden by the survivor.
2026-05-09 22:11 UTC · MODE B · claude/keen-goldberg-BdF7x · respect prefers-reduced-motion in the open-station incoming-train ring pulse: hold the breathing wave at its mean (0.5) so the ring still renders at its average size/opacity — preserving the "which trains are headed here, with imminent ones glowing brighter" signal — but no longer oscillates. Marker LERP stays informational and untouched.
2026-05-09 23:50 UTC · MODE B · claude/esc-dismiss-panels-2026-05-09 · keyboard a11y: ESC now dismisses the topmost open custom bottom sheet (Search → Station → Line → Nearby → More → cinematic follow). Priority order extracted as a pure helper (`lib/escDismiss.ts`) with a node-env regression test guarding against silent reordering. Radix-managed dialogs (LiveTrainsPopup, MoreSheet's Alerts/About) keep their own ESC handler — they stop propagation so the window listener never fires for them.
2026-05-10 00:15 UTC · MODE B · claude/nearby-walk-minutes-2026-05-10 · StationRow distance line now shows "5 min walk · 320 m" instead of "320 m away" — Apple-Maps idiom that surfaces the unit a NYC rider actually budgets in. New pure helper formatWalkSummary in lib/stopsIndex.ts shares the existing 1.4 m/s × 1.3 grid-detour pedestrian model with walkMinutes / catchVerdict; sub-minute case (meters === 0) falls back to the plain "X m away" so a rider standing on the entrance doesn't see a misleading "0 min walk".
2026-05-10 02:17 UTC · MODE B · claude/keen-goldberg-Wl1oC · StationPanel arrival rows now surface per-train staleness textually: when a trip's underlying VehiclePosition hasn't reported in 90 s+ an amber "Updated Nm ago" / "Stale · Nm" sub-line appears beneath the route, mirroring FollowCapsule's eyebrow swap so the row-level signal matches the cinematic-mode capsule. Trips with no Train entry (stop_time_update without a paired VehiclePosition) skip the chrome since the prediction is by definition as fresh as the latest poll. New jsdom RTL test for ArrivalRow covers fresh / soft-stale / hard-stale / null branches.
2026-05-10 04:10 UTC · MODE B · claude/station-alerts-2026-05-10 · StationPanel now surfaces active service alerts scoped to the complex's serving routes — extracts the existing collapsed-by-default AlertsSection out of LinePanel into components/AlertsSection.tsx so both panels share the same chrome, then drops it into StationPanel between the title row and the live arrivals scroll. A rider opening a station now sees the same heads-up the line view gives them ("no Q service downtown this weekend") instead of having to drill into a line first. New jsdom RTL test for AlertsSection covers count pluralization, top-severity tinting, the start-collapsed disclosure, and the 8-alert cap.
2026-05-10 06:25 UTC · MODE B · claude/keen-goldberg-9MHSE · NearbyPanel StationRow arrivals are now direction-balanced — at a busy station like Times Square during rush hour the soonest three trains can all be Northbound, hiding Southbound entirely. New pure helper `pickBalancedArrivals` in lib/topArrivals.ts guarantees one per direction when both are running within ~30 min, otherwise falls back to soonest-by-time so a 35-min Southbound doesn't bump a useful 3-min Northbound. 12 unit tests cover the empty / single-direction / naturally-balanced / forced-balance / horizon-fallback / symmetry branches.
2026-05-10 06:30 UTC · MODE B · claude/keen-goldberg-BF8Xa · TripPlanRow's "Next: 3m · 7m · 12m" inline list now flips each ETA amber when its train's VehiclePosition has slipped past the 90 s freshness floor, plus surfaces a single "Updated Nm ago" / "Stale · Nm" sub-line beneath — same trainStaleness band logic as the recent ArrivalRow + FollowCapsule changes, picking the soonest stale entry so the band matches the train the rider is most likely to chase. Threaded lastReportedByTripId + generatedAtSec from useTrains through both call sites (NearbyPanel's GoingToCard hero and SearchSheet's directions list) so the signal lands on every trip-plan surface. New panelUI.test.tsx covers fresh / soft-stale / hard-stale / no-train-entry / props-omitted / onSelect branches.
2026-05-10 07:15 UTC · MODE B · claude/nearby-empty-state-2026-05-10 · NearbyPanel's empty-state ternary previously rendered nothing for geo.status === "error" (watch timed out, POSITION_UNAVAILABLE) and "unavailable" (no Geolocation API / insecure context). A first-time visitor whose iOS Safari geolocation watch failed was getting a blank panel with no path forward. Extracted the chain into a named NearbyEmptyState export, added an "error" branch with a "Try again" button that re-fires geo.request(), and an "unavailable" branch that points at MoreSheet for manual Home/Work setup. New components/NearbyPanel.test.tsx covers all 7 branches (idle CTA, prompting/no-fix, prompting/post-fix silent, denied no-button, error retry, unavailable no-button, granted silent).
2026-05-10 07:45 UTC · MODE B · claude/trip-plan-catch-verdict-2026-05-10 · TripPlanRow's "Next: 2m · 7m · 12m" inline list now applies catchVerdict tinting when walkFromMeters > 0: a missed train (eta − now < runnable buffer) renders strikethrough-gray, a runnable one renders amber, walkable emerald, chill neutral — same VERDICT_STYLES idiom StationRow already uses on the same data. estimateTripTimeSec also skips uncatchable arrivals when walkFromMeters > 0 so totalMin no longer silently uses a 60s ETA the rider is a 4-min walk away from. Existing stale-tinting falls back when there's no walk (rider on platform); leadStale sub-line still surfaces "Updated Nm ago" textually when both apply. New unit tests for both files (3 commuteRouting cases + 4 panelUI cases) cover miss/run/chill bands + the all-uncatchable fallback.
2026-05-10 07:55 UTC · MODE B · claude/line-opengraph-image-2026-05-10 · per-line OG cards: build-time PNG per /line/[id] showing a hero-scale MTA bullet (line color), the route's full name (e.g. "Broadway-Seventh Avenue Local"), and the from↔to terminals + station count. Mirrors the per-station OG idiom: same gradient, same generateStaticParams pattern (keyed off Object.keys(lines), which is routeIds, so shuttles route through GS/FS/H rather than the display "S"), no edge runtime since `lib/stations.server.ts`'s GTFS reader needs node:fs. Auto-attached by Next's file-convention; no explicit metadata wiring needed. Changelog bullet updated to mention /line alongside the existing /about, /pricing, /changelog OG cards.
2026-05-10 08:18 UTC · MODE B · claude/status-opengraph-image-2026-05-10 · per-page OG card for /status — mirrors the chrome of the existing marketing OGs (/about, /pricing, /changelog, /press) with a "Status" surface pill, a "Live system health." headline, and a triple-dot row labeled MTA feed · Static data · Runtime so a /status link previewed in chat/social reads as a real status surface rather than the generic site card. Static dots in calm-default emerald (the OG can't reflect live state — outages still surface on the page itself). Edge runtime, no fs. Changelog bullet updated to mention /press and /status alongside the existing entries.
2026-05-10 10:05 UTC · MODE B · claude/keen-goldberg-TQLUO · StationPanel alerts now respect alert.stopIds: a station-scoped alert ("No [R] at Cortlandt St") only surfaces at the listed station instead of every R station. New alertsForStation helper in lib/useAlerts.ts: when an alert lists stopIds, filter by station-stop intersection; when stopIds is empty, fall back to route intersection so genuine line-wide notices still surface everywhere. Six new unit tests cover line-wide / station-scoped / facility-only / multi-stop / null-data / no-match branches.
2026-05-10 10:30 UTC · MODE B · claude/keen-goldberg-OkxMb · per-page OG cards for /privacy and /terms — the last two marketing surfaces still falling back to the generic site OG. Indigo+emerald tint for /privacy with the "No accounts. No tracking. No ads." headline; amber+slate tint for /terms with "Plain English. No surprises." Mirrors the existing edge-runtime ImageResponse pattern from /about, /pricing, /changelog, /press, completing the marketing-OG sweep. Changelog bullet updated to mention /privacy + /terms alongside the existing entries.
2026-05-10 14:15 UTC · MODE B · claude/system-pulse-fleet-staleness-2026-05-10 (PR #73) · LiveTrainsPopup (System Pulse) now aggregates per-vehicle staleness across the live fleet — new summarizeFleetStaleness helper in lib/trainStaleness.ts walks data.trains using the existing 90 s / 360 s thresholds, returning { stale, veryStale }; the dialog renders an amber footer line under the "Right now" status grid ("12 trains haven't reported in 90 s+ · 3 stale (6 m+)") only when stale > 0. The snapshot-age signal already shown at the header was misleading on its own: a fresh poll can still carry minutes-old vehicle positions. Now the fleet-level summary agrees with the marker fade and arrival-row sub-line riders see at the per-train level. Three new node-env tests cover empty / mixed-fleet / generatedAt-fallback branches.
2026-05-10 16:19 UTC · MODE B · claude/keen-goldberg-LTJh3 · AlertsSection now auto-expands on first mount when at least one alert is severe (full suspensions, "no service") so a rider opening a station/line panel during a route-wide outage sees the disruption headline without an extra tap. Warning + info stay collapsed by default — the "elevator out" / "delays" framing is routine and shouldn't push live arrivals off-screen for every station on the line. The 28dvh max-height cap on the expanded list still bounds growth, and parents already pass a stable `key` (lineId / stopId) so context-switching to a quieter station correctly remounts and re-collapses. Two new RTL tests cover the severe auto-expand path and the warning-only / info-only collapsed-by-default paths; the existing "starts collapsed" test still passes since it uses an info-severity default.
2026-05-10 17:15 UTC · MODE B · claude/alerts-dialog-severe-autoexpand-2026-05-10 · Service Alerts dialog (the global one reachable from MoreSheet → Alerts) now auto-expands AlertItem cards when severity === "severe": a rider opening the dialog during an active suspension sees the description body unfolded on mount instead of having to tap each chevron. Same severity-driven disclosure principle PR #74 applies at the AlertsSection wrapper level on station/line panels — calm default for warning + info, proactive for severe — but at the per-card level inside the global dialog. Severe stays rare in the MTA feed (NO_SERVICE only), so the unfolded body doesn't drown the list. New components/AlertsButton.test.tsx covers info/warning/severe defaults, the no-body fallback, and explicit-collapse-after-auto-expand.
2026-05-10 19:15 UTC · MODE B · claude/keen-goldberg-ASOai (PR #76) · align LinePanel snapshot-staleness with the SubwayMap live-pill + LiveTrainsPopup System Pulse — threshold from 30 s to 60 s (kills the false-positive band a recovered /api/trains hiccup left in the eyebrow for ~10 s) and swaps the binary "· stale" for a numeric label ("Stale · 90s" rounded to 10 s sub-minute, "Stale · 2m" past 120 s). New pure helper `snapshotStaleLabel(ageSec)` in lib/trainStaleness.ts next to the existing per-train `trainStaleness` so calling code makes the snapshot-vs-per-train distinction explicit. Four node-env tests cover the calm-default band, sub-minute rounding ladder, minutes branch, and non-finite-input defense.
2026-05-10 20:18 UTC · MODE B · claude/keen-goldberg-YTLx6 · two raw `console.warn` calls were bypassing `lib/observability.ts` despite CLAUDE.md's "errors flow through observability" convention — `lib/useAlerts.ts` swallowed alerts-poll fetch failures and `components/RegisterSW.tsx` swallowed service-worker registration errors. Both now route through `captureException` so a rider whose SW silently fails to register (cache quota, locked storage, CSP edge case) or whose alerts feed has stalled becomes visible to the operator via the same /api/log sink that already catches other client errors. New jsdom test (`lib/useAlerts.observability.test.ts`, 2 cases) mocks the shim and asserts `captureException(err, { source: "useAlerts" })` for fetch-rejection + HTTP-error branches.
2026-05-10 22:15 UTC · MODE B · claude/trip-plan-trunk-catch-verdict-2026-05-10 · trunk-route TripPlanRow (siblings present, e.g. N/R/W) now applies catchVerdict tinting on each upcoming-arrival ETA — matching what PR #67 did for the single-route branch. The trunk branch was previously the only path where a rider walking up got stale-only tinting and no miss/run/walk feedback. Bullet stays its real route color; only the ETA chip picks up the verdict. Three new RTL tests cover the miss strikethrough, the no-walk neutral case, and the stale-amber fallback.
2026-05-10 22:55 UTC · MODE A · claude/search-revamp-2026-05-10 · user flagged the search empty-state ("Where are you going?") as visually crowded — a big Quick-Commute card crowned over two small Home/Work pills crowned over a Recent list, three different visual idioms for the same one-tap-to-a-place intent. Revamped to a flat Apple-Maps-style row list across three sections: Favorites (Commute / Home / Work, only those that exist), Nearby stations (top 3 closest complexes with route bullets and walk-summary, gated on read-only geo), and Recent. Removes the redundancy between the big Quick-Commute card (explicit Home→Work) and the small Home pill (current→Home), surfaces actionable nearby stations the rider can tap to open or set as an anchor, and frees vertical space so Recent isn't pushed under the iOS keyboard's autocomplete bar. Also dropped the "miss" verdict pill on `DirectionArrivalsRow` since the line-through on the ETA already conveys "you missed it" — frees enough horizontal space for the third arrival per direction to fit instead of wrapping when both miss+walk land on the same row.
2026-05-10 23:10 UTC · MODE B · claude/keen-goldberg-11Yxs · /status page poller (StatusPanel) now pauses on hidden tab + on offline, matching the useTrains + useAlerts pattern — a backgrounded /status tab was hitting /api/health every 15 s in perpetuity, and an airplane-mode device was firing into the void each tick. Pause/resume hooks: visibilitychange + subscribeOnline (mirror of useTrains' bind sequence), with an immediate tick on resume so the rider sees a fresh timestamp the moment they return. Header tail-label swaps the timestamp for "Offline · paused" when navigator.onLine flips false, and the cold-start placeholder swaps "Checking systems…" for an explicit "Offline — health checks paused" so a rider opening /status while offline doesn't see an indefinite spinner. New jsdom RTL test (app/status/StatusPanel.test.tsx, 6 cases) covers the mount-fires-once happy path, hidden-on-mount silence, offline-on-mount silence + placeholder, online-event resume, visibilitychange resume, and the loaded-then-offline tail-label swap.
2026-05-11 00:20 UTC · MODE B · claude/keen-goldberg-k88Y6 · the three decorative "live-feed" ping rings — SubwayMap header pill, LiveTrainsPopup System Pulse dot, and StatusPanel "all systems ok" dot — were the last `animate-ping` instances in the app still firing for riders with `prefers-reduced-motion`. Swapped to Tailwind's `motion-safe:animate-ping` variant so the outward pulse only runs when the OS hasn't asked us to dampen motion. The underlying colored dot (with shadow glow) still carries the state signal, so reduced-motion riders read "live" the same way — just without the decorative emphasis ring. No tests added: this is a pure CSS-variant prefix swap that jsdom can't meaningfully resolve. Changelog updated.
2026-05-11 02:10 UTC · MODE B · claude/motion-safe-pulse-2026-05-10 · loading-skeleton `animate-pulse` sweep — the seven auto-firing pulses still bypassing reduced-motion (Service Alerts list skeletons in both inline + dialog renderers, StationPanel + LinePanel "Loading live arrivals…" placeholders, NearbyPanel prompting-branch Navigation icon, MapView "Loading map…" first-paint label, LinePicker bullet-grid placeholder discs) now use `motion-safe:animate-pulse`. The static gray shapes + aria-hidden chrome + "Loading…" copy still convey loading state for riders with `prefers-reduced-motion`; the breathing animation only runs for everyone else. Continues the sweep started by PR #82 on the decorative ping rings. SearchSheet's `animate-spin` on the refresh icon stays unconditional — user-initiated feedback animations are exempt under WCAG. No new tests (CSS-variant prefix swap, jsdom can't resolve the media query meaningfully).
2026-05-11 04:30 UTC · MODE B · claude/keen-goldberg-j1d7v (PR #84) · service-alert cards now surface their active window — a compact "Until Sun 5 AM" / "Ends in 45 min" sub-line under the headline so a rider reading "No [Q] service" knows when service returns. The /api/alerts route already plumbed startTime + endTime through ServiceAlert; the gap was strictly in rendering. New pure helper `lib/alertWindow.ts` formats the window pinned to America/New_York (a rider in another tz reads NYC wall-clock, not their device's), with branches for ≤60 min countdown / same-day clock / within-week weekday / >7 days month+day / future-scheduled "Starts" / null for indefinite (>14d) windows. Wired into both AlertItem renderers (AlertsSection inline card + AlertsButton global dialog); parent owns one useNow(open, 30s) tick so we don't pay for a timer per row, and AlertsButton's Dialog converted from uncontrolled to controlled useState(open) so the timer can be gated on visibility. 19 new tests (14 node-env helper branches + 3 AlertsButton + 2 AlertsSection).
2026-05-11 06:25 UTC · MODE B · claude/line-page-interchange-bullets-2026-05-11 · /line/[id] station list now surfaces interchange-line bullets next to each stop instead of repeating the current-line bullet on every row — the page header already tells the rider which line they're on, so 30+ identical "A" bullets next to 30 stop names was noise. Apple/Google Maps transit idiom is to surface the *transfer* lines; single-line stops render no bullets, hub complexes (Times Sq, Union Sq, Atlantic-Barclays) flex-wrap a full transfer strip. New pure helper `getInterchanges(entry, currentRouteId)` in lib/lineInterchanges.ts filters by routeId (not display id) so a shuttle landing page correctly drops only itself instead of every display-"S" route. Five node-env tests cover empty entry / only-current-line / multi-route ordering / shuttle routeId filter / non-mutation.
2026-05-11 07:05 UTC · MODE B · claude/drag-handle-component-2026-05-11 · UI cleanup audit pass — the bottom-sheet drag-handle pill (a 36px-wide × 5px-tall capsule crowning every panel for mobile drag-to-resize) was duplicated literally across StationPanel, LinePanel, NearbyPanel, SearchSheet, MoreSheet: identical button wrapper (`sm:hidden flex items-start justify-center h-7 pt-1.5 flex-shrink-0 touch-none w-full` + onClick + aria-label) wrapping an identical `w-9 h-[5px] rounded-full bg-white/25` pill. Lifted into `components/DragHandle.tsx` (props: onTap, ariaLabel) — five panels now share one source of truth, so any future change to the hit-area size, pill geometry, or motion treatment is a one-line edit. LinePicker's decorative pill is left alone: it lives inside a Dialog header with absolute positioning over the title row, structurally different from the panel-crown role. No rider-visible behavior change; no tests added (stateless wrapper, already exercised through each panel's existing tests). The audit also surfaced other candidates (severity-tint dedup, fmtEta divergence per surface) but they're either borderline 2-site duplications or intentionally-different formats per panel context, so deferred.
2026-05-12 04:11 UTC · MODE B · PR #107 · drop rider IP (`forwardedFromIp`) from `/api/log`'s persisted record — the rate-limiter at `lib/rateLimit.ts` keys off it in-memory only (Map resets on cold start), so the abuse defense doesn't need the IP to land in the operator's server log sink. /privacy promises no server-side rider-identifying data; this closes the last visible gap. New route test pins both the canonical field name + a structural sweep against any IP-shaped string value so a future re-introduction under any name trips the suite.
2026-05-11 17:20 UTC · MODE B · claude/keen-goldberg-HBdjy · trip-planner leg-2 wait now uses live arrivals at the transfer station. estimateTripTimeSec previously used a constant FALLBACK_WAIT_S (4 min) for every transfer leg even when arrivalsByStation contained the transfer complex; only leg 1 picked up live data. Refactored to track `arrivalAtBoardSec` across legs (rider's modeled clock at the current leg's board, advanced by prior wait + travel + TRANSFER_S) so leg 2+ looks up the next reachable train at boardComplexId from that timestamp. Same sibling-route awareness (N/R/W on Broadway BMT etc.) as leg 1. Catchability filter stays gated on `i === 0 && walkFromMeters > 0` (origin walk-up physics); transfer legs use the simpler `eta < arrivalAtBoardSec - 5` skip since TRANSFER_S already prices in platform-to-platform overhead. The leg-1 eta semantics (eta-from-now, walk time double-counted into total separately) are preserved exactly — only the transfer-leg branch changes. Result: rankPlansByTime now distinguishes a fast L→4 transfer (4 arriving 1m after rider reaches Union Sq) from a slow L→4 (next 4 is 12m out) when both share the same nominal stop count, which previously tied. Four new tests cover the live-transfer happy path, the leg-2 arrival-already-left fallback, sibling-route counting at the transfer, and wrong-route/direction filtering.
2026-05-11 07:45 UTC · MODE A · claude/product-narrowing-2026-05-11 · user asked for a senior-PM read on the app and agreed with the framing — the product reads as three stacked surfaces (beautiful-map / utility-app / diagnostic-tool) without picking one, the Pricing page promises unshipped features ("Pro is TBD"), and the rider's More overflow is cluttered with marketing/legal links a rider in the middle of catching a train doesn't need. Four narrowing changes in one PR: (1) NearbyPanel no longer auto-opens on cold boot — `useState(true)` → `useState(false)` — so the first impression is the live map, not a half-covered viewport asking for geolocation; the iOS Safari geo prompt still has a clean user-activated path the moment the rider taps the Near-me button. (2) /pricing deleted entirely (page + opengraph-image + sitemap entry + footer + nav link); its "we're free, no ads, you can self-host" copy moved into a new "Free forever" section on /about, including the forward-looking note that Pro is a future opt-in layer. (3) /press deleted entirely; the boilerplate paragraph + brand assets + press contact pointer condensed into a "For press & fact-checks" section at the bottom of /about. (4) MoreSheet's bottom marketing-link strip (About · Changelog · Privacy · Terms · Feedback) removed — the section above already exposes About (in-app dialog) + Send feedback + GitHub as full-width rows, and Privacy/Terms belong on the marketing site footer for visitors who land there from a search result, not on the rider's overflow menu. Just the version-label footer remains. Companion changes: MarketingShell header drops Pricing nav link; sitemap loses /pricing + /press; layout.tsx route-list comment trimmed.
2026-05-12 02:15 UTC · MODE B · claude/privacy-push-notif-disclosure-2026-05-12 · /privacy page hadn't been updated for the recently-shipped push-notifications feature (PRs #93/#95/#96/#97) — the "Short version" still asserted "stored only in your browser's localStorage. They never leave your device.", and the "Your rights (GDPR / CCPA)" section claimed "stores nothing about you on its servers, there is no profile to export, correct, or delete." Both true before the Postgres-backed push-subscription row landed, both false after. Same accuracy/trust axis as PR #98 (changelog refresh) and PR #104 (/about copy fix), applied to the legal surface. Three additions: (1) a fourth Short-version bullet flagging push as the explicit server-side exception, (2) a new "Push notification subscriptions (opt-in)" subsection under "What is collected" with a literal field-by-field readout (anonymous UUID, push endpoint URL, p256dh + auth keys, subscribed routes, timestamps) sourced from the actual migration so the disclosure can't drift from the schema, plus the 30-day unsubscribed-purge + 14-day dispatch-log retention windows from CLAUDE.md, (3) the "What is stored on your device" list gains the anonymous notification ID bullet, and (4) the "Your rights" section rewritten from a single paragraph into three concrete revoke paths (toggle off in MoreSheet → 30-day hard-delete, revoke geolocation, clear site data). LAST_UPDATED bumped to today. Copy-only, no test added (consistent with PR #98 and PR #104). Changelog intentionally untouched — PR #98 is open and editing the same 0.9.0 entry; a second edit would conflict. Marketing-surface refinement = tier 4.

2026-05-12 00:25 UTC · MODE B · claude/keen-goldberg-XqULa · /api/health 'static' check is no longer a hardcoded "ok" sentinel — replaced with a fs.statSync probe of public/gtfsData.json (missing → "down"; below the 100 KB floor → "down" with size detail; healthy → "ok" with size in KB). Every panel in the client app depends on this 429 KB blob, so a deploy that lost the build step previously rendered the live map but left every panel empty while /status kept reporting "all systems operational" — uptime monitors had nothing to page on. Now the rollup correctly flips to 503 and the X-Health-Status header surfaces "down" without parsing the body. Five node-env tests cover the ok rollup, missing-file branch, truncated-file branch, MTA-failure-not-masked-by-passing-static regression guard, and the routing-header contract.
2026-05-11 23:11 UTC · MODE B · claude/about-push-notifications-2026-05-11 · /about copy drifted from the shipped product after this week's push-notifications work (#93, #95, #96, #97) — the page advertised "push alerts on your saved lines" as a future *Pro* layer feature, but opt-in push notifications for severe service alerts already ship today on the free baseline (MoreSheet → Notifications, severity = "severe" fanout). Two fixes on the same page: (1) added a "What you can do with it" bullet for the shipped feature so a first-time visitor learns about it on the way in; (2) rephrased the Free-forever Pro-layer sentence to make the per-line opt-in v2 the future feature (layered over the free severe-tier baseline) rather than implying no push alerts are free yet. The "leave at X" reminders + Apple Watch fragments of the Pro-layer sentence are unchanged — those remain unshipped.
2026-05-11 22:18 UTC · MODE B · claude/morsheet-station-name-2026-05-11 · MoreSheet's Home/Work commute rows showed a generic "Pinned station" placeholder for any station-kind anchor — the lookup-against-the-index branch the comment in `endpointLabel` flagged as TODO was never wired up. SearchSheet had already solved the same problem locally (its own `stationsByComplexId.get(home.stopId)?.name` resolve at lines 1308/1338), but it owns a full station-index build for unrelated reasons. Lifting that into MoreSheet would have been overkill — the sheet only needs one name, not the route-badge + complex-merge pipeline. New tiny pure helper `stationNameByStopId(lines, stopId)` in `lib/stopsIndex.ts` scans `Object.values(lines)` directly (≤1k iterations on the full network, well under a millisecond) and returns the name or null. Falls back to the existing "Pinned station" string only on the cold-boot window before `/public/gtfsData.json` has hydrated, or for a stopId no longer in the network — both states are visibly recoverable. Five tests cover present-stopId / non-first-line stopId / unknown-stopId / null-lines / empty-stopId branches.

2026-05-11 16:10 UTC · MODE B · claude/notif-error-feedback-2026-05-11 · push-subscription error feedback — usePushSubscription now returns a surface-ready `error: string | null` and NotificationsRow renders it in an aria-live="alert" region below the row. Three concrete failure paths now produce rider-visible copy instead of silently rolling back the spinner: (1) /api/notifications/subscribe non-2xx → "Couldn't enable notifications. Try again." (operator detail still captured via captureException); (2) NEXT_PUBLIC_VAPID_KEY missing at runtime → "Notifications aren't set up on this deploy." so a self-hoster sees the misconfig in the UI, not just in logs; (3) /api/notifications/unsubscribe non-2xx → "Couldn't disable notifications. Try again." plus a behavior fix — the hook no longer proceeds to the browser-side `pushManager.unsubscribe()` and flip state to "off" when the server-side delete fails, since that previously left the rider subscribed server-side while the UI claimed otherwise (next severe alert would still ping them). Error clears at the start of the next subscribe/unsubscribe call. Permission-prompt "default" (user dismissed) is intentionally not an error — they declined. Four new tests on top of the existing 12 cover: error string on subscribe HTTP-500, error cleared on next successful attempt, VAPID-missing branch, unsubscribe-fails-server stays subscribed. Two new NotificationsRow tests cover the visible-error render and the mounted-but-sr-only empty branch (AT pairs that only announce changes inside a pre-existing live region).

2026-05-11 14:14 UTC · MODE B · claude/proxy-error-paths-2026-05-11 · /api/geocode + /api/walk now normalize the three failure modes the previous `await fetch` chain let escape as Next.js 500s: client AbortError (typeahead keystroke cancelling the previous suggest, or rider closing the trip panel mid-directions-fetch) returns 499 with the empty shape and no log entry — that one's normal client behavior, logging would just be noise; network-level rejection (DNS, TCP, TLS, ECONNRESET) returns 502 + empty shape and emits a single captureWarning so clustered upstream incidents surface in the operator log sink; Mapbox malformed-JSON responses (5xx HTML pages, partial dropped bodies) also land 502 + empty shape + warn instead of throwing out of `upstream.json()`. Upstream non-2xx (401, 429) status is still preserved verbatim so the client's typeahead + walkingDirections cache-drop logic still trips. The geocode proxy's two action branches share a single `proxyMapbox` helper now. Eight new node-env tests: four geocode (AbortError silent / network 502 / malformed-JSON 502 / 429 preserved) + six walk (coord 400 short-circuit, plus AbortError / network / malformed / non-2xx / happy-path verbatim forward). Drive-by: walk now has a route.test.ts; previously the proxy had no test coverage at all.

2026-05-11 11:10 UTC · MODE B · claude/live-pill-aria-2026-05-11 · SubwayMap live-feed pill state transitions (offline → connecting → degraded → stale → live) were silent to screen readers — the button's aria-label only fires on focus, so a rider with VoiceOver/NVDA never heard the feed flip. Mirrors PR #94's /status treatment: new pure helper `lib/liveFeedState.ts` reduces the four-axis state (online · hasData · feedDegraded · stale) to one ordinal name, plus a `liveFeedAnnouncement` formatter that returns a count-free sentence so the parallel sr-only `role="status"` region doesn't fire on every 8 s poll tick (the count change). 11 node-env tests cover all 5 states + priority order + count-omission invariant. Existing visual chrome chain on the pill untouched; the announcement region is additive.

2026-05-11 10:08 UTC · MODE B · claude/changelog-refresh-2026-05-11 · /changelog had drifted from reality after the product narrowing in #91 (Pricing + Press pages deleted) and the three push-notifications PRs that shipped in #93/#95/#96: it still advertised "Press kit at /press" as an added entry, listed /pricing and /press among the per-page Open Graph cards, and was completely silent on opt-in push alerts for severe service alerts — the most user-visible feature shipped this week. Three edits on the 0.9.0 entry: dropped the press-kit bullet, trimmed the OG-card bullet to the routes that still exist (/about, /changelog, /status, /privacy, /terms, /line/[id]), and prepended one `added` row for notifications + one `removed` row covering the page deletions. The `removed` type was already declared in TYPE_LABELS/TYPE_TINT but no entry had ever used it — first instance picks up the rose tint already wired in.
2026-05-11 08:15 UTC · MODE B · claude/status-aria-live-2026-05-11 · /status page now announces health transitions to screen readers — the rollup headline pair ("All systems operational" / "Some systems are degraded" / "Major outage in progress" + the tone-label sub-line) sits in a role="status" container so a state change is re-read aloud as one atomic block; scoped tight to the inner pair so the per-15s timestamp + version updates on the sibling node don't trigger noisy repeat announcements. Cold-start "Checking systems…" placeholder and offline-paused notice are also role="status" so a screen-reader rider landing on /status hears the page state without scanning for content. A /api/health fetch failure surfaces as role="alert" (assertive) — a rider hitting /status to confirm health needs to hear "endpoint unreachable" without waiting for a polite queue to flush. Drive-by: the placeholder's `animate-pulse` picks up the `motion-safe:` prefix used by the rest of the loading-skeleton sweep, closing the last gap from PR #83. Three new tests (rollup polite-region, offline placeholder is a status, fetch-failure is an alert).
2026-05-12 06:10 UTC · MODE B · claude/station-page-line-links-2026-05-12 (PR #109) · /station/[slug] SEO surface had asymmetric internal linking — /line/[id] already links into every station on the line, but station pages linked back to nothing. Both surfaces (header bullet pill row + "Lines at this station" list) now wrap each train in a Link to /line/[lineSlug]; the list rows pick up the same rounded-card hover idiom the Nearby-stations list directly below already uses. Drive-by accuracy fix: the pre-existing `lines[r.id]` lookup silently returned undefined for shuttle stations (Lines record is keyed by routeId, GS/FS/H all display as "S"), dropping the line name from the description text below the badge — switched to `lines[r.routeId]` with a comment documenting the divergence. JSX-only change to a static SEO page with no existing test file; no new tests, consistent with PR #98 / #104 precedent. Build catches a broken Link / missing import — all 451 station pages prerender clean.

2026-05-12 19:14 UTC · MODE B · claude/keen-goldberg-UCbGt · MarketingShell exposed two unlabelled landmark regions — the header `<nav>` had no aria-label, and the footer link cluster (About/Changelog/Status/Privacy/Terms/GitHub) was a bare `<div>` so it wasn't even a landmark. A screen-reader rider on /about /privacy /terms /changelog /status (or /station/[slug] and /line/[id], which also wrap MarketingShell) heard two "navigation" regions with no name, or — worse, in the footer case — a flat list of links with no programmatic grouping. Header nav now carries `aria-label="Primary"`; footer cluster lifted into `<nav aria-label="Footer">`. Mirrors PR #110's landmark-labeling pass on the bottom-sheet panels. One new RTL test (`components/marketing/MarketingShell.test.tsx`) asserts both labels and that they're distinct so a future refactor that drops one trips the suite.
2026-05-12 08:12 UTC · MODE B · claude/keen-goldberg-mCBcl · the five bottom-sheet panels (StationPanel, LinePanel, NearbyPanel, SearchSheet, MoreSheet) rendered as bare `<div>`s with no landmark role — a screen-reader rider navigating by landmarks couldn't jump to the open panel, and the heading inside it wasn't programmatically scoped to a containing region. Added `role="region"` + `aria-label` on each panel root: "{name} station", "{id} line", "Near me", "Search and directions", "More". Deliberately NOT `role="dialog"` — the panels are non-modal (the map underneath stays interactive on desktop, gestures dismiss on mobile) and don't trap focus; claiming dialog semantics without focus management would mislead AT users worse than no role at all. Inline comment on each panel anchors the role/label decision. UI-attribute-only change consistent with the "where practical" caveat in CLAUDE.md — adding full-panel render harnesses for each of 5 panels just to assert the new attribute would 10× the diff for no signal beyond what `tsc --noEmit` already catches. Matches the precedent set by PRs #82/#83 (motion-safe variant sweep, also pure CSS/attribute additions without panel render tests).
2026-05-13 06:15 UTC · MODE B · claude/keen-goldberg-TtHnC · InstallPrompt banner was the only `role="dialog"` left in the codebase outside the Radix-backed real dialogs (LinePicker, LiveTrainsPopup, AlertsButton, MoreSheet). The banner is non-modal — no focus trap, no ESC-to-dismiss, doesn't move focus on mount, the map underneath stays interactive — so claiming dialog semantics mislead AT users into expecting a contained surface they have to exit. Same antipattern PR #110 documented + avoided when it landed `role="region"` on the five bottom-sheet panels. Switched `role="dialog"` → `role="region"` (keeping the existing `aria-label="Install StandClear"` so the region is named in the landmark rotor) and added an inline comment matching PR #110's idiom so a future refactor doesn't "fix" it back to dialog without wiring focus management + ESC. JSX-attribute-only edit; no test added, consistent with PR #110's precedent ("the existing checks already cover render and tsc --noEmit catches typos").
2026-05-12 20:09 UTC · MODE B · claude/refresh-spin-motion-safe-2026-05-12 (PR #118) · the SearchSheet trip-planner refresh-button spinner was the last `animate-spin` left without a `motion-safe:` prefix after PRs #82/#83 swept the rest of the loading-affordance idiom. globals.css:473 already collapses every animation under prefers-reduced-motion to 0.01ms/iteration-count 1, so the icon doesn't visibly spin under reduced motion either way — the change is pure pattern-consistency, plus it spares the browser allocating an animation it would immediately collapse to one frame. Comment on the strip extended to spell out the reduced-motion trade-off (static icon; the re-ranked trip list underneath is the refresh signal). JSX-attribute-only edit, no test added — same precedent as PRs #82/#83.
2026-05-12 14:15 UTC · MODE B · claude/homepage-jsonld-2026-05-12 · The two SEO surfaces /station/[slug] and /line/[id] ship inline JSON-LD (TrainStation + BusOrSubwayRoute) but the canonical homepage `/` — the surface that actually ranks for "NYC subway tracker" — shipped no structured data at all, so Google had no machine-readable anchor for the brand-level WebApplication entity the per-page TrainStation/BusOrSubwayRoute entries should be hanging off. New `lib/seoSchemas.ts` exports a single `homepageJsonLd()` helper returning a typed WebApplication block with name/url/description/applicationCategory=TravelApplication/isAccessibleForFree=true/offers={price:"0",priceCurrency:"USD"}/featureList/author+publisher; both free-app fields (isAccessibleForFree and offers.price) are set deliberately because Google's rich-result template reads either one depending on the surface. Wired into app/page.tsx as an inline `<script type="application/ld+json">` so the crawler gets the block in the SSR payload, not after hydration. 4 node-env tests cover the WebApplication shape contract, the both-fields-for-free duplication, the schema.org type pick, and JSON-serialization round-trip. Server-only JSON-LD injection into the static-prerendered `/` route — no client bundle delta.
2026-05-12 17:10 UTC · MODE B · claude/breadcrumb-jsonld-2026-05-12 (PR #116) · Station + line landing pages had a single inline JSON-LD block each (TrainStation / BusOrSubwayRoute), but no BreadcrumbList — so Google rendered the raw URL slug as the SERP breadcrumb path. Station slugs carry the MTA stop_id suffix to disambiguate same-named complexes (e.g. `14-st-union-sq-635`, `14-st-132`, `aqueduct-n-conduit-av-h11`), which read as noise in the search result. Added a second inline `<script type="application/ld+json">` per page with a flat two-item BreadcrumbList: site home → page (display-name leaf). Existing JSON-LD blocks untouched; multi-script per page is explicitly supported by Google's structured-data docs and preserves the existing transit rich-results behaviour. Flat two-level because no /stations or /lines index page exists to act as a category-level crumb. Inlined alongside the existing literal (no helper, no new module) to match the inline-JSON-LD pattern PR #114 is establishing for the homepage WebApplication — if `lib/seoSchemas.ts` lands there, a future run can consolidate. No test (consistent with PR #109/#111/#114/#115 precedent for inline data blocks on static SEO pages). Changelog intentionally untouched — PR #112 is open and editing 0.9.0, a second edit would conflict.
2026-05-12 16:20 UTC · MODE B · PR #115 · add app/not-found.tsx so typo'd URLs + every notFound() throw from /station/[slug] and /line/[id] land on a brand-consistent surface instead of Next's default unstyled 404. Mirrors error.tsx's visual idiom (subway emoji + eyebrow + headline + Open the map / About actions); robots noindex/follow same as /status. Build promotes /_not-found from default fallback to a static-prerendered route. No test added — JSX-only static page, build catches broken Link / missing import, consistent with PRs #98/#104/#109/#111 precedent.
2026-05-12 10:14 UTC · MODE B · claude/keen-goldberg-lWWsu · /station/[slug] "Nearby stations" rows showed bare meters (`{Math.round(meters)} m`) on the right while the in-app NearbyPanel renders the same neighbor list as "5 min walk · 320 m" via `formatWalkSummary`. SEO surface and live app now share the unit a NYC rider actually budgets in — walking minutes first, meters as a secondary spatial cue — and the helper already encodes the same 1.3× grid-detour factor as catchVerdict + the trip planner, so the three surfaces stay aligned. Sort order unchanged (walkMinutes is monotonic in haversine meters at constant grid factor); intro copy trimmed from "ranked by straight-line distance" → "ranked by distance" since the label no longer reads as a raw straight-line meters value. JSX-only edit to a static SEO page; no test added, consistent with PR #98 / #104 / #109 precedent. Build prerenders all 451 station pages clean.
2026-05-13 00:15 UTC · MODE B · claude/station-slug-test-coverage-2026-05-13 (PR #121) · lib/stationSlug.ts had zero direct test coverage despite being the URL-stability contract for the SEO surface (sitemap.xml emits 451 entries, app/station/[slug]/{page,opengraph-image}.tsx prerender on it via generateStaticParams + use findStationBySlug per-request, app/line/[id]/page.tsx builds internal Links from it). New lib/stationSlug.test.ts (node-env, 16 cases) pins five invariants: (1) shape `<name-kebab>-<stopId-lower>` with the canonical examples from the source comment verbatim, (2) stopId case-folding (R20→r20, A32→a32, L03→l03 — every letter-prefixed MTA stopId is uppercase upstream), (3) punctuation collapsing using real station names from public/gtfsData.json (Lexington Av/53 St, Cathedral Pkwy (110 St), Prince's Bay, St Mary's St) plus the synthetic & → and rule the source documents but no shipped name exercises today, (4) trim+collapse rules (multiple separators → one hyphen; leading/trailing hyphens stripped), (5) round-trip identity findStationBySlug(index, stationSlug(s)) === s for every fixture entry plus same-name-across-boroughs disambiguation (Broadway G40 vs M16). Same shape as PRs #119 (rateLimit) and #120 (mtaAlerts.severityOf): no production code change, just closing a coverage gap on a load-bearing utility so a future kebab/case refactor trips the suite instead of silently re-shaping 451 URLs.
2026-05-12 22:10 UTC · MODE B · claude/ratelimit-test-coverage-2026-05-12 · lib/rateLimit.ts had zero test coverage despite being the abuse-defense floor for three API routes (/api/geocode, /api/walk, /api/log). The module encodes three subtle invariants that are easy to regress in: (1) sliding-window semantics — keep raw timestamps, not a counter, so the window never resets all at once and creates a double-rate burst at the boundary; (2) blocked-requests-don't-record — rejected calls must NOT extend the window's expiry, otherwise a saturating attacker keeps the limiter pegged indefinitely with no chance for a legitimate caller to age back in; (3) MAX_ENTRIES eviction — when the keyed Map hits 5_000 distinct callers, the oldest 10% is dropped in insertion order, bounding memory under sustained scanning. New lib/rateLimit.test.ts (node-env, 11 cases): 6 sliding-window cases (accept-under, reject-at, slide-on-expiry, blocked-don't-extend, per-key isolation, re-accept after full expiry), 1 eviction case (seed 5000 keys → add 5001st → verify k-0..k-499 evicted while k-500 retained), 4 callerKey cases (leftmost from chain, whitespace trim, single value, anon fallback). Used vi.useFakeTimers + vi.advanceTimersByTime for time-travel, and vi.resetModules + per-describe loadFresh() for module-state isolation across cases. Tier 1 reliability — fills a coverage gap that no other run has addressed.
2026-05-13 02:15 UTC · MODE B · claude/keen-goldberg-Iz0yc · `lib/observability.ts` had partial direct test coverage — the existing suite covered the forward path (sendBeacon vs fetch fallback, info skip, FORWARD_BUDGET cap) but left three load-bearing helpers untested: `sanitize` (URL query-string redaction — PII boundary between Mapbox typeahead errors and the operator log sink), `safeMessage` (catch-block `unknown` → string coercion), and `safeStack` (Error-only stack capture). 10 new tests added in two describe blocks via the existing forward-payload observable, plus a shared `installMocks` / `teardownMocks` / `readBeaconRecord` harness to keep each block self-contained. sanitize: 6 cases (URL+query → `${origin}${pathname}?[redacted]`, URL-no-query passthrough, plain string passthrough, non-string passthrough, malformed http(s) URL passthrough, console.error tag is also redacted mid-pipeline so an operator scanning Vercel logs never sees a raw address). captureException: 4 cases (Error.message+stack, plain string with no stack, plain-object JSON-stringify, circular-ref String() fallback — pins the `try/catch` around JSON.stringify so a future refactor that drops it doesn't crash every catch site). 415 tests pass (was 405, +10).
2026-05-12 23:15 UTC · MODE B · PR #120 · lib/mtaAlerts.ts::severityOf is the shared severity classifier between the rider-facing alerts panel (severity-tinted badges in StationPanel/LinePanel/AlertsButton/status) and the push-notification dispatch (lib/pushDispatch filters to severity==="severe" before fanning out via web-push). Zero direct test coverage before this run — only the implicit kind the surrounding alerts route exercised at a glance. 26 new node-env cases in lib/mtaAlerts.test.ts pin: (1) effect-driven trust — when MTA's GTFS-RT enum is informative (NO_SERVICE / SIGNIFICANT_DELAYS / REDUCED_SERVICE / DETOUR / MODIFIED_SERVICE / STOP_MOVED) the header is NOT consulted, so a DETOUR with "no service" in the headline stays warning; (2) header-scan priority — severe-phrase checks (`^no \[`, ` no service`, `suspended`, `significant delay`) run before warning-phrase checks (`delays`, `rerouted`, `runs express`, `runs local`, `bypass`, `skipping`, `every `) so an alert that mentions both lands on severe; (3) severe-phrase narrowness — `/^no \[/` and ` no service` both gate on the bracketed-route prefix MTA actually uses, so a bare standalone `no service` lands info (regression guard against future loosening); (4) future-proof header fallback for GTFS-RT effect values we haven't classified yet. Same shape as PR #119 — pure-logic coverage closer on a load-bearing utility, no production code change. Misclassification has two trust-eroding failure modes: silently dropping a severe alert from the push fanout, or escalating routine chatter to severe and waking every subscriber.
2026-05-13 08:12 UTC · MODE B · claude/keen-goldberg-f5aey · /api/alerts was the only API route under app/api/ without a route.test.ts file — the eight siblings (trains, geocode, walk, health, log, notifications/{subscribe,unsubscribe,stats}, cron/{dispatch-alerts,cleanup-subscriptions}) all have one. Added four node-env tests covering the route's contract: forwards the AlertsResponse body verbatim, surfaces the empty-array soft-fail (lib/mtaAlerts.ts swallows fetch + decode errors so the client never sees a 5xx on a transient MTA hiccup — PR #123 pinned that on the lib side; this pins the route side), pins the public,max-age=30,stale-while-revalidate=120 edge-cache header (a regression that drops it would silently 10x our upstream fan-out), and guards the application/json content-type so a future swap from NextResponse.json to `new Response(...)` doesn't break consumers. fetchActiveAlerts is mocked rather than the upstream fetch — protobuf decode + severity classification are exercised by the lib tests (#120/#123), so duplicating them here would just be brittle coverage. Closes the API-route coverage gap.
2026-05-13 10:15 UTC · MODE B · claude/keen-goldberg-MvXud · `lib/useSheetDrag.ts` (304 lines, the shared drag-to-dismiss / detent state machine that backs StationPanel, LinePanel, NearbyPanel, SearchSheet, and MoreSheet) had no test coverage — the only untested non-trivial hook left in `lib/` outside of the in-flight test-PRs (#119/#120/#121/#122/#123/#125). New jsdom test file `lib/useSheetDrag.test.tsx` covers the state machine via synthesized pointer events: starts-at-half resting state, drag-down past `dismissPx` fires `onDismiss`, drag-up past `commitPx` from half expands to full, drag-down past commit from full collapses back to half, `dismissOnDrag=false` collapses instead of dismissing (the SearchSheet's anti-misfire path), short drag below thresholds leaves detent unchanged, pointerdown on a `<button>` / `[data-no-drag]` child skips drag init (so the close/star/route-bullet taps register normally), `onPointerCancel` resets state without firing detent change or dismiss, `open=false` re-renders reset detent + drag flags, `onHandleTap` toggles detents and the one-shot guard swallows the post-drag synthetic tap once, and the desktop layout (matchMedia(max-width: 639px) = false) emits no transform and silently no-ops pointer handlers. 13 tests, suite goes from 405 → 418 passing.
2026-05-13 17:14 UTC · MODE B · PR #128 · `components/StationPanel.tsx` and `components/FollowCapsule.tsx` shipped a private byte-identical `fmtEta` helper each, but with divergent unit signatures — StationPanel's took `(eta: number, now: number)` with `now` in millis (divided inside the function), while FollowCapsule's took `(etaSec: number, nowSec: number)` and forced the conversion at the call site (`fmtEta(arrival.eta, now / 1000)`). Same string output today, but two functions named identically with mismatched unit contracts are a foot-gun for the third caller, and the at-call-site division in FollowCapsule was already a small wart. New `lib/etaFormat.ts` exports `formatEtaCountdown(etaSec, nowMs)` with the canonical `(etaSec, nowMs)` shape — picks up the dominant time-shape across the codebase (`Date.now()` / `useNow()`) so a future caller doesn't have to re-derive the unit convention. Header comment deliberately calls out which other formatters in the codebase stay separate and why: `LinePanel`'s 30 s "Now" + minute-only granularity is intentional ("calm at distance" choice for the dense corridor view), `panelUI`'s "Xs"/"Xm" short form is intentional for the compact list rows, and `useTrainMarkers`'s inline glyph format is intentional for the map overlay — so a future "consolidate all the things" sweep doesn't strip per-surface UX intent without first re-justifying it. 11 node-env tests pin the boundaries: Now window (secs <= 5 incl. negative remaining and 5.4 s rounding to Now), seconds window (6/30/59 + 5.6 rounding up to 6 sec), minutes window (60 boundary, 89 rounds down to 1 min, 90 rounds up to 2 min — guarding against a Math.floor regression for riders who budget 90 s as still 2 min away), and three unit-convention cases that pin the canonical `(etaSec, nowMs)` shape so a future caller that swaps either parameter unit trips the suite. Suite goes 515 → 526 passing.
2026-05-13 19:10 UTC · MODE B · claude/keen-goldberg-NY4ek · `components/InstallPrompt.tsx` (183 lines, the add-to-home-screen banner that gates on platform sniff + 60s show-delay + localStorage dismiss) had no test coverage — the only non-trivial untested user-facing component left in `components/` outside of the four large interactive shells (SubwayMap, SearchSheet, MoreSheet, LinePanel) and the Mapbox-driven MapView/useTrainMarkers. New `components/InstallPrompt.test.tsx` (jsdom, 11 cases) pins five invariants: (1) the 60s show-delay — banner is hidden at `SHOW_DELAY_MS - 1` and revealed at `SHOW_DELAY_MS`; (2) the standalone-PWA suppression — both `matchMedia("(display-mode: standalone)").matches=true` and the iOS-specific `navigator.standalone=true` path; (3) the mobile-only gate — desktop UA never sees the banner; (4) the localStorage `standclear:a2hs-dismissed:v1` honor — pre-set flag hides the banner across visits; (5) the iOS-vs-Android branch divergence — iOS UA shows the share-sheet "Add to Home Screen" instruction with no native Install button; Android UA shows generic copy, then reveals the "Install app" button only after a `beforeinstallprompt` event fires. Plus: Install-app click calls `deferred.prompt()` + writes the dismiss flag + unmounts; X-button click writes the flag + unmounts without invoking prompt; `role="region"` is asserted distinct from `role="dialog"` (pins the PR #117 / 2026-05-13 06:15 routine-log decision that the banner is non-modal and shouldn't claim dialog semantics). Used `vi.useFakeTimers` for the 60s delay + `Object.defineProperty(window.navigator, "userAgent")` + `window.matchMedia = vi.fn()` to mock platform sniff, matching the harness already in `lib/usePushSubscription.test.ts` and `lib/useSheetDrag.test.tsx`. Suite 510 → 521 (+11).
2026-05-13 04:16 UTC · MODE B · PR #123 · `lib/mtaAlerts.ts::fetchActiveAlerts` had no direct test coverage — only the implicit kind the dispatch cron + alerts route exercise via real MTA bytes in production. PR #120 covers `severityOf`; this run closes the surrounding pipeline. New `lib/mtaAlerts.fetchActive.test.ts` (node-env, 20 cases) lifts the real `gtfs-realtime-bindings` protobuf encoder to build round-trippable fixtures (mirroring `app/api/trains/route.test.ts`) and pins five invariants: active-window filter (past/future/multi-period/always-on), per-selector AND shape preserved across multiple informedEntity (PR #71 Codex P1), parent-stop suffix strip (`R23N`/`R23S` → `R23`) in both aggregate and selector, `Long.toNumber()` conversion for 32-bit-overflow timestamps, and the error fallback — HTTP non-2xx, fetch rejection, and malformed protobuf bytes all land empty AlertsResponse + captureException with `{what,url}`. Filename intentionally sibling-of (`.fetchActive.test.ts`) so this run does not race PR #120 on the same path; mirrors `useAlerts.observability.test.ts` + `useAlerts.test.ts` coexisting. Drive-by: empty-`entity.id` → `alert-{n}` fallback pinned (load-bearing for `alert_dispatch_log` PK uniqueness in `lib/pushDispatch.ts`); en translation preferred + first-translation fallback when no en. One brittle theoretical edge case (period with unbounded `end` falling through to Infinity) deliberately not pinned — proto3 numeric default materializes the missing Long as 0, so the documented `?? Infinity` fallback never fires on a real round-trip and the MTA feed always emits both endpoints anyway.
2026-05-13 20:14 UTC · MODE B · claude/keen-goldberg-XhfQL · /line/[id] had only the per-stop transfer bullets (rendered next to each row by getInterchanges) — no top-of-page connections summary. A rider scanning the line page had to read every station row to discover which other routes the line connects to anywhere along the route. Apple Maps + Google Maps both surface a "Connections" overview on a transit-line view; this run adds the same idiom. New `aggregateInterchanges()` helper in lib/lineInterchanges.ts unions getInterchanges across every stop on the line, dedup'd by routeId with first-appearance ordering preserved, then /line/[id] renders the result as a "Direct transfers" section between the "About the {id} train" paragraph and "Stations on this line" — w-9 h-9 pill bullets matching the /station/[slug] header style, each Link wrapping to `/line/<lineSlug(routeId)>`. Closes the bidirectional SEO loop: /station/[slug] already cross-links into /line/[id] (PR #109) and station rows on /line/[id] already cross-link into /station/[slug] — but until now the only line→line crosslinks were buried inside the per-row transfer pills as non-links (routes shown but not clickable). Now every line landing page links directly to every other line it can transfer to. Dedup key is routeId (matches getInterchanges's filter) so a shuttle landing page (routeId GS/FS/H, display "S") drops only its own bullet, never collides with another display-"S" route. 7 new tests on aggregateInterchanges (empty, all-current, dedup+order, undefined-skip, shuttle filter, shape passthrough, no-mutate); 51 files / 517 tests pass (was 510, +7). JSX edit on a static prerendered surface — build catches broken Link / missing import; all 25 line pages still prerender.
